define(function() {
    "use strict";

    var SearchController;
    SearchController = (function() {
        function SearchController() {}

        SearchController.prototype.setSearchAction = function() {

        };

        return SearchController;
    }());

    return SearchController;
});